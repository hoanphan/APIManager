# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'APIManager' do
  use_frameworks!
  pod 'RxSwift'
  
  # Networking
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'Moya' 
  
  pod 'SwiftLint'
end
